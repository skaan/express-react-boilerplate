import React from 'react';
import { connect } from 'react-redux';

import {} from '../../actions';

const HomeView = () => {
  return <div>HomeView</div>;
};

const mapToProps = () => {
  return {};
};

export default connect(mapToProps, {})(HomeView);
