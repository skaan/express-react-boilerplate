import React from 'react';
import { connect } from 'react-redux';

import {} from '../../actions';

const LoginView = () => {
  return <div>LoginView</div>;
};

const mapToProps = () => {
  return {};
};

export default connect(mapToProps, {})(LoginView);
